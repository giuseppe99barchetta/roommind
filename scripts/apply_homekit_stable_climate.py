from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]


def replace_once(path: str, old: str, new: str) -> None:
    p = ROOT / path
    text = p.read_text(encoding="utf-8")
    if old not in text:
        raise SystemExit(f"anchor not found in {path}: {old[:120]!r}")
    p.write_text(text.replace(old, new, 1), encoding="utf-8")


path = "custom_components/roommind/climate.py"
replace_once(
    path,
    '''    @property\n    def supported_features(self) -> ClimateEntityFeature:\n        caps = self._capabilities()\n        mode = self.hvac_mode\n        features = ClimateEntityFeature.TURN_ON | ClimateEntityFeature.TURN_OFF\n\n        # Temperature controls must describe the currently selected operating\n        # mode, not every capability the room happens to have. In particular,\n        # OFF and FAN_ONLY have no meaningful temperature setpoint.\n        if mode in (HVACMode.AUTO, HVACMode.HEAT, HVACMode.COOL, HVACMode.DRY):\n            features |= ClimateEntityFeature.TARGET_TEMPERATURE\n\n        if caps.fan_modes:\n            features |= ClimateEntityFeature.FAN_MODE\n        if caps.swing_modes:\n            features |= ClimateEntityFeature.SWING_MODE\n        if caps.swing_horizontal_modes:\n            features |= ClimateEntityFeature.SWING_HORIZONTAL_MODE\n        return features\n\n    @property\n    def target_temperature(self) -> float | None:\n        mode = self.hvac_mode\n        if mode in (HVACMode.OFF, HVACMode.FAN_ONLY):\n            return None\n        heat, cool = self._logical_targets()\n        if mode == HVACMode.AUTO:\n            return (heat + cool) / 2.0\n        return cool if mode in (HVACMode.COOL, HVACMode.DRY) else heat\n''',
    '''    def _target_control_mode(self) -> HVACMode | None:\n        """Return the thermal mode whose target should remain visible to clients."""\n        room = self._room() or {}\n        selected = room.get("room_hvac_mode")\n        if selected == "heat_cool":\n            selected = "auto"\n        if selected in ("auto", "heat", "cool", "dry"):\n            return HVACMode(selected)\n\n        last = room.get("room_last_hvac_mode")\n        if last == "heat_cool":\n            last = "auto"\n        if last in ("auto", "heat", "cool", "dry") and HVACMode(last) in self.hvac_modes:\n            return HVACMode(last)\n\n        if HVACMode.AUTO in self.hvac_modes:\n            return HVACMode.AUTO\n        if HVACMode.COOL in self.hvac_modes:\n            return HVACMode.COOL\n        if HVACMode.HEAT in self.hvac_modes:\n            return HVACMode.HEAT\n        return None\n\n    @property\n    def supported_features(self) -> ClimateEntityFeature:\n        caps = self._capabilities()\n        features = ClimateEntityFeature.TURN_ON | ClimateEntityFeature.TURN_OFF\n\n        # Capabilities must be stable across runtime state changes. HomeKit builds\n        # its accessory characteristics from this bitmask when the accessory is\n        # created, so dropping TARGET_TEMPERATURE while OFF/FAN_ONLY leaves a\n        # permanently incomplete accessory until it is recreated.\n        if any(mode in caps.hvac_modes for mode in ("auto", "heat", "cool", "dry")):\n            features |= ClimateEntityFeature.TARGET_TEMPERATURE\n\n        if caps.fan_modes:\n            features |= ClimateEntityFeature.FAN_MODE\n        if caps.swing_modes:\n            features |= ClimateEntityFeature.SWING_MODE\n        if caps.swing_horizontal_modes:\n            features |= ClimateEntityFeature.SWING_HORIZONTAL_MODE\n        return features\n\n    @property\n    def target_temperature(self) -> float | None:\n        mode = self._target_control_mode()\n        if mode is None:\n            return None\n        heat, cool = self._logical_targets()\n        if mode == HVACMode.AUTO:\n            return (heat + cool) / 2.0\n        return cool if mode in (HVACMode.COOL, HVACMode.DRY) else heat\n''',
)

replace_once(
    path,
    '''    async def async_set_temperature(self, **kwargs: Any) -> None:\n        selected = self.hvac_mode\n        if selected in (HVACMode.OFF, HVACMode.FAN_ONLY):\n            raise ValueError(f"Temperature cannot be set while RoomMind is in {selected.value} mode")\n\n        heat, cool = self._logical_targets()\n''',
    '''    async def async_set_temperature(self, **kwargs: Any) -> None:\n        current_mode = self.hvac_mode\n        inactive = current_mode in (HVACMode.OFF, HVACMode.FAN_ONLY)\n        selected = self._target_control_mode() if inactive else current_mode\n        if selected is None:\n            raise ValueError("Room has no temperature-controllable HVAC mode")\n\n        heat, cool = self._logical_targets()\n''',
)

replace_once(
    path,
    '''        if selected in (HVACMode.HEAT, HVACMode.COOL, HVACMode.DRY):\n            self._manual_activation_guard(selected.value)\n        store = self.coordinator.hass.data[DOMAIN]["store"]\n        mode = selected.value if selected != HVACMode.OFF else "heat_cool"\n        await store.async_update_room(\n            self._area_id,\n            {\n                "logical_heat_target": heat,\n                "logical_cool_target": cool,\n                "room_hvac_mode": mode,\n                "override_heat": heat if mode in ("heat", "heat_cool", "auto") else None,\n                "override_cool": cool if mode in ("cool", "heat_cool", "auto") else None,\n                "override_until": None,\n                "override_type": OVERRIDE_CUSTOM,\n            },\n        )\n        # A setpoint changed through the canonical entity is an explicit user\n        # command (HA/HomeKit). Persisting it is not enough: forward it to the\n        # physical device that is currently responsible for that direction.\n        await self._async_apply_manual_temperature(selected, heat, cool)\n        await self.coordinator.async_request_refresh()\n''',
    '''        store = self.coordinator.hass.data[DOMAIN]["store"]\n        if inactive:\n            # OFF/FAN_ONLY still expose a usable setpoint to HA/HomeKit, but a\n            # setpoint edit must never wake hardware or change the selected mode.\n            await store.async_update_room(\n                self._area_id,\n                {\n                    "logical_heat_target": heat,\n                    "logical_cool_target": cool,\n                },\n            )\n            await self.coordinator.async_request_refresh()\n            return\n\n        if selected in (HVACMode.HEAT, HVACMode.COOL, HVACMode.DRY):\n            self._manual_activation_guard(selected.value)\n        mode = selected.value\n        await store.async_update_room(\n            self._area_id,\n            {\n                "logical_heat_target": heat,\n                "logical_cool_target": cool,\n                "room_hvac_mode": mode,\n                "room_last_hvac_mode": mode,\n                "override_heat": heat if mode in ("heat", "auto") else None,\n                "override_cool": cool if mode in ("cool", "auto") else None,\n                "override_until": None,\n                "override_type": OVERRIDE_CUSTOM,\n            },\n        )\n        # A setpoint changed through the canonical entity is an explicit user\n        # command (HA/HomeKit). Persisting it is not enough: forward it to the\n        # physical device that is currently responsible for that direction.\n        await self._async_apply_manual_temperature(selected, heat, cool)\n        await self.coordinator.async_request_refresh()\n''',
)

replace_once(
    path,
    '''        mode = hvac_mode.value\n        updates = {"room_hvac_mode": mode, "override_until": None, "override_type": OVERRIDE_CUSTOM}\n        updates["override_heat"] = heat if mode in ("heat", "heat_cool", "auto") else None\n        updates["override_cool"] = cool if mode in ("cool", "heat_cool", "auto") else None\n        if mode == "off":\n            updates.update({"override_heat": None, "override_cool": None, "override_type": None})\n''',
    '''        mode = hvac_mode.value\n        room = self._room() or {}\n        updates = {"room_hvac_mode": mode, "override_until": None, "override_type": OVERRIDE_CUSTOM}\n        updates["override_heat"] = heat if mode in ("heat", "auto") else None\n        updates["override_cool"] = cool if mode in ("cool", "auto") else None\n        if mode == "off":\n            previous = room.get("room_hvac_mode")\n            if previous == "heat_cool":\n                previous = "auto"\n            if previous in ("auto", "heat", "cool", "dry"):\n                updates["room_last_hvac_mode"] = previous\n            updates.update({"override_heat": None, "override_cool": None, "override_type": None})\n        elif mode in ("auto", "heat", "cool", "dry"):\n            updates["room_last_hvac_mode"] = mode\n''',
)

replace_once(
    path,
    '''        if mode != "auto":\n            self._manual_activation_guard(mode)\n            await self._async_apply_manual_hvac_mode(mode, heat, cool)\n        await self.coordinator.hass.data[DOMAIN]["store"].async_update_room(self._area_id, updates)\n        await self.coordinator.async_request_refresh()\n''',
    '''        if mode != "auto":\n            self._manual_activation_guard(mode)\n            await self._async_apply_manual_hvac_mode(mode, heat, cool)\n        await self.coordinator.hass.data[DOMAIN]["store"].async_update_room(self._area_id, updates)\n        if mode != "off":\n            await self._async_apply_stored_ac_options()\n        await self.coordinator.async_request_refresh()\n''',
)

replace_once(
    path,
    '''    async def async_turn_on(self, **kwargs: Any) -> None:\n        fallback = HVACMode.AUTO if HVACMode.AUTO in self.hvac_modes else HVACMode.HEAT\n        await self.async_set_hvac_mode(fallback)\n\n    async def _set_ac_option(self, key: str, service: str, service_key: str, value: str) -> None:\n        acs = get_ac_eids((self._room() or {}).get("devices", []))\n        if not acs:\n            raise ValueError("Room has no AC device")\n        store = self.coordinator.hass.data[DOMAIN]["store"]\n        await store.async_update_room(self._area_id, {key: value})\n        for entity_id in acs:\n            await self.coordinator.hass.services.async_call(\n                "climate", service, {"entity_id": entity_id, service_key: value}, blocking=True\n            )\n''',
    '''    async def async_turn_on(self, **kwargs: Any) -> None:\n        room = self._room() or {}\n        last = room.get("room_last_hvac_mode")\n        if last == "heat_cool":\n            last = "auto"\n        try:\n            preferred = HVACMode(last) if last else None\n        except ValueError:\n            preferred = None\n        if preferred not in self.hvac_modes or preferred == HVACMode.OFF:\n            preferred = HVACMode.AUTO if HVACMode.AUTO in self.hvac_modes else next(\n                (mode for mode in self.hvac_modes if mode != HVACMode.OFF), HVACMode.OFF\n            )\n        await self.async_set_hvac_mode(preferred)\n\n    async def _async_apply_stored_ac_options(self) -> None:\n        """Apply fan/swing settings that may have been edited while the room was off."""\n        room = self._room() or {}\n        acs = get_ac_eids(room.get("devices", []))\n        if not acs:\n            return\n        options = (\n            ("room_fan_mode", "set_fan_mode", "fan_mode"),\n            ("room_swing_mode", "set_swing_mode", "swing_mode"),\n            ("room_swing_horizontal_mode", "set_swing_horizontal_mode", "swing_horizontal_mode"),\n        )\n        for key, service, service_key in options:\n            value = room.get(key)\n            if not value:\n                continue\n            for entity_id in acs:\n                await self.coordinator.hass.services.async_call(\n                    "climate", service, {"entity_id": entity_id, service_key: value}, blocking=True\n                )\n\n    async def _set_ac_option(self, key: str, service: str, service_key: str, value: str) -> None:\n        acs = get_ac_eids((self._room() or {}).get("devices", []))\n        if not acs:\n            raise ValueError("Room has no AC device")\n        store = self.coordinator.hass.data[DOMAIN]["store"]\n        await store.async_update_room(self._area_id, {key: value})\n        if self.hvac_mode == HVACMode.OFF:\n            return\n        for entity_id in acs:\n            await self.coordinator.hass.services.async_call(\n                "climate", service, {"entity_id": entity_id, service_key: value}, blocking=True\n            )\n''',
)

# Update old expectations and add focused HomeKit-compatible regressions.
test_path = ROOT / "tests/test_climate.py"
text = test_path.read_text(encoding="utf-8")
text = text.replace(
    '''def test_canonical_fan_only_and_off_hide_single_temperature_target(mock_coordinator):\n    """Modes without a thermal setpoint must not expose one in the HA climate card."""''',
    '''def test_canonical_fan_only_and_off_keep_last_temperature_target(mock_coordinator):\n    """Inactive modes keep a stable thermal target for HA/HomeKit clients."""''',
)
text = text.replace(
    '''    assert entity.target_temperature is None\n\n    room["room_hvac_mode"] = "off"\n    assert entity.target_temperature is None\n\n    room["room_hvac_mode"] = "cool"\n    assert entity.target_temperature == 26.0\n''',
    '''    assert entity.target_temperature == 26.0\n\n    room["room_hvac_mode"] = "off"\n    assert entity.target_temperature == 26.0\n\n    room["room_hvac_mode"] = "cool"\n    assert entity.target_temperature == 26.0\n''',
    1,
)
text = text.replace(
    '''def test_canonical_fan_only_hides_all_temperature_controls(mock_coordinator):''',
    '''def test_canonical_fan_only_keeps_stable_temperature_feature(mock_coordinator):''',
)
text = text.replace(
    '''    assert entity.target_temperature is None\n    assert entity.target_temperature_low is None\n    assert entity.target_temperature_high is None\n    assert not entity.supported_features & ClimateEntityFeature.TARGET_TEMPERATURE\n    assert not entity.supported_features & ClimateEntityFeature.TARGET_TEMPERATURE_RANGE\n    assert entity.supported_features & ClimateEntityFeature.FAN_MODE\n''',
    '''    assert entity.target_temperature == 26.0\n    assert entity.target_temperature_low is None\n    assert entity.target_temperature_high is None\n    assert entity.supported_features & ClimateEntityFeature.TARGET_TEMPERATURE\n    assert not entity.supported_features & ClimateEntityFeature.TARGET_TEMPERATURE_RANGE\n    assert entity.supported_features & ClimateEntityFeature.FAN_MODE\n''',
    1,
)
old = '''@pytest.mark.asyncio\nasync def test_canonical_fan_only_rejects_temperature_changes(mock_coordinator):\n    coordinator, store = mock_coordinator\n    coordinator.hass.states.get.return_value = MagicMock(\n        state="fan_only",\n        attributes={"hvac_modes": ["off", "cool", "fan_only"]},\n    )\n    store.get_room.return_value = _canonical_room(\n        [{"entity_id": "climate.ac", "type": "ac"}], room_hvac_mode="fan_only"\n    )\n    store.async_update_room = AsyncMock()\n    entity = RoomMindClimate(coordinator, "living_room")\n\n    with pytest.raises(ValueError, match="fan_only"):\n        await entity.async_set_temperature(temperature=23.0)\n    store.async_update_room.assert_not_awaited()\n'''
new = '''@pytest.mark.asyncio\nasync def test_canonical_fan_only_temperature_change_is_stored_without_waking_ac(mock_coordinator):\n    coordinator, store = mock_coordinator\n    coordinator.hass.states.get.return_value = MagicMock(\n        state="fan_only",\n        attributes={"hvac_modes": ["off", "cool", "fan_only"]},\n    )\n    store.get_room.return_value = _canonical_room(\n        [{"entity_id": "climate.ac", "type": "ac"}], room_hvac_mode="fan_only"\n    )\n    store.async_update_room = AsyncMock()\n    entity = RoomMindClimate(coordinator, "living_room")\n\n    await entity.async_set_temperature(temperature=23.0)\n\n    store.async_update_room.assert_awaited_once_with(\n        "living_room", {"logical_heat_target": 21.0, "logical_cool_target": 23.0}\n    )\n    coordinator.hass.services.async_call.assert_not_called()\n'''
if old not in text:
    raise SystemExit("fan-only test anchor not found")
text = text.replace(old, new, 1)
text += '''\n\ndef test_canonical_off_keeps_temperature_capability_and_last_target(mock_coordinator):\n    coordinator, store = mock_coordinator\n    coordinator.hass.states.get.return_value = MagicMock(\n        state="off",\n        attributes={"hvac_modes": ["off", "heat", "cool"], "fan_modes": ["auto", "low"]},\n    )\n    store.get_room.return_value = _canonical_room(\n        [{"entity_id": "climate.ac", "type": "ac"}],\n        room_hvac_mode="off",\n        room_last_hvac_mode="cool",\n        logical_cool_target=27.0,\n    )\n    entity = RoomMindClimate(coordinator, "living_room")\n\n    assert entity.hvac_mode == HVACMode.OFF\n    assert entity.supported_features & ClimateEntityFeature.TARGET_TEMPERATURE\n    assert entity.supported_features & ClimateEntityFeature.FAN_MODE\n    assert entity.target_temperature == 27.0\n\n\n@pytest.mark.asyncio\nasync def test_canonical_off_temperature_change_never_activates_hardware(mock_coordinator):\n    coordinator, store = mock_coordinator\n    coordinator.hass.states.get.return_value = MagicMock(\n        state="off", attributes={"hvac_modes": ["off", "cool"]}\n    )\n    store.get_room.return_value = _canonical_room(\n        [{"entity_id": "climate.ac", "type": "ac"}],\n        room_hvac_mode="off",\n        room_last_hvac_mode="cool",\n        logical_cool_target=26.0,\n    )\n    store.async_update_room = AsyncMock()\n    entity = RoomMindClimate(coordinator, "living_room")\n\n    await entity.async_set_temperature(temperature=27.0)\n\n    store.async_update_room.assert_awaited_once_with(\n        "living_room", {"logical_heat_target": 21.0, "logical_cool_target": 27.0}\n    )\n    coordinator.hass.services.async_call.assert_not_called()\n\n\n@pytest.mark.asyncio\nasync def test_canonical_turn_on_restores_last_mode(mock_coordinator):\n    coordinator, store = mock_coordinator\n    room = _canonical_room(\n        [{"entity_id": "climate.ac", "type": "ac"}],\n        room_hvac_mode="off",\n        room_last_hvac_mode="cool",\n    )\n    store.get_room.return_value = room\n    store.async_update_room = AsyncMock()\n    coordinator.hass.states.get.return_value = MagicMock(\n        state="off", attributes={"hvac_modes": ["off", "cool"]}\n    )\n    entity = RoomMindClimate(coordinator, "living_room")\n    entity._manual_activation_guard = MagicMock()\n    entity._async_apply_manual_hvac_mode = AsyncMock()\n    entity._async_apply_stored_ac_options = AsyncMock()\n\n    await entity.async_turn_on()\n\n    entity._async_apply_manual_hvac_mode.assert_awaited_once_with("cool", 21.0, 26.0)\n    entity._async_apply_stored_ac_options.assert_awaited_once()\n\n\n@pytest.mark.asyncio\nasync def test_fan_option_edited_while_off_is_deferred(mock_coordinator):\n    coordinator, store = mock_coordinator\n    store.get_room.return_value = _canonical_room(\n        [{"entity_id": "climate.ac", "type": "ac"}],\n        room_hvac_mode="off",\n        room_last_hvac_mode="cool",\n    )\n    store.async_update_room = AsyncMock()\n    coordinator.hass.states.get.return_value = MagicMock(\n        state="off", attributes={"hvac_modes": ["off", "cool"], "fan_modes": ["low", "high"]}\n    )\n    entity = RoomMindClimate(coordinator, "living_room")\n\n    await entity.async_set_fan_mode("high")\n\n    store.async_update_room.assert_awaited_once_with("living_room", {"room_fan_mode": "high"})\n    coordinator.hass.services.async_call.assert_not_called()\n'''
test_path.write_text(text, encoding="utf-8")

print("HomeKit stable climate capability patch applied")
