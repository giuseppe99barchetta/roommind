name: Run climate mode UI semantics fix

on:
  push:
    branches: [main]
    paths:
      - scripts/apply_climate_mode_ui_semantics.py
      - .github/workflows/run-climate-mode-ui-semantics.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply fix
        run: python scripts/apply_climate_mode_ui_semantics.py
      - name: Validate Python
        run: python -m compileall -q custom_components/roommind tests
      - name: Install test dependencies
        run: pip install -r requirements-dev.txt
      - name: Run focused climate tests
        run: pytest -q tests/test_climate.py
      - name: Commit result
        run: |
          rm .github/workflows/run-climate-mode-ui-semantics.yml scripts/apply_climate_mode_ui_semantics.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: expose temperature controls by climate mode"
          git push origin HEAD:main
